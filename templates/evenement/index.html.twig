{% extends 'layout.html.twig' %}

{% block title %}Evenement index{% endblock %}

{% block body %}
    <h1>Evenement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Prix_event</th>
                <th>Descrption_event</th>
                <th>Photo_event</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evenement in evenements %}
            <tr>
                <td>{{ evenement.prixEvent }}</td>
                <td>{{ evenement.descrptionEvent }}</td>
                <td>{{ evenement.photoEvent }}</td>
                <td>
                    <a href="{{ path('evenement_showA', {'id': evenement.id}) }}">show</a>
                    <a href="{{ path('evenement_editA', {'id': evenement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('evenement_newA') }}">Create new</a>
{% endblock %}
