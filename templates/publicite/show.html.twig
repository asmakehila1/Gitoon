{% extends 'layout.html.twig' %}

{% block title %}Publicite{% endblock %}

{% block body %}

    <div class="row page-titles">
        <div class="col-md-5 align-self-center">
            <ol class="breadcrumb">
                <li class="breadcrumb-item active"><a href="{{ path('publicite_index') }}">Publicité</a></li>
            </ol>
        </div>
        <div class="col-md-7 align-self-center text-right">
            <div class="d-flex justify-content-end align-items-center">

                <a href="{{ path('publicite_new') }}" type="button" class="btn btn-info "><i
                            class="fa fa-plus-circle"></i> Ajouter </a>

            </div>
        </div>
    </div>

    <div class="row">
        <!-- Column -->
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    =                    <div class="row">
                        <div class="col-lg-3 col-md-3 col-sm-6">
                            <div class="white-box text-center"><img src="{{ asset('uploads/') ~ publicite.imagePub }}"
                                                                    class="img-responsive"></div>
                        </div>

                        <div class="col-lg-12 col-md-12 col-sm-12">
                            <h3 class="box-title m-t-40">Informations Publicité </h3>
                            <div class="table-responsive">
                                <table class="table">
                                    <tbody>
                                    <tr>
                                        <th>Id</th>
                                        <td>{{ publicite.id }}</td>
                                    </tr>
                                    <tr>
                                        <th>Titre publicité </th>
                                        <td>{{ publicite.titrePub }}</td>
                                    </tr>
                                    <tr>
                                        <th>Description publicité </th>
                                        <td>{{ publicite.descriptionPub }}</td>
                                    </tr>
                                    </tbody>
                                </table>
                                {{ include('publicite/_delete_form.html.twig') }}
                                <a href="{{ path('publicite_edit', {'id': publicite.id}) }}" class="btn btn-alert">Modifier</a>
                                <div>
                                    <a href="{{ path('publicite_index') }}">Retour à la liste </a>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Column -->
    </div>

{% endblock %}
