{% extends 'base.html.twig' %}

{% block title %}Temoin{% endblock %}

{% block body %}
    <h1>Temoin</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ temoin.id }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ temoin.titre }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ temoin.description }}</td>
        </tr>
        <tr>
            <th>Lien</th>
            <td>{{ temoin.lien }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('temoin_index') }}">back to list</a>
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ temoin.id }}</td>
        </tr>
        <tr>
            <th>Prix_event</th>
            <td>{{ evenement.prixEvent }}</td>
        </tr>
        <tr>
            <th>Descrption_event</th>
            <td>{{ evenement.descrptionEvent }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('evenement_indexA') }}">back to list</a>

    <a href="{{ path('evenement_editA', {'id': evenement.id}) }}">edit</a>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}