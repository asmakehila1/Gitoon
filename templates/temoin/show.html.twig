{% extends 'baseback.html.twig' %}

{% block title %}Temoin{% endblock %}

{% block body %}
    <h1>Temoin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoin.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ temoin.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ temoin.description }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ temoin.lien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('temoin_index') }}">back to list</a>

    <a href="{{ path('temoin_edit', {'id': temoin.id}) }}">edit</a>

    {{ include('temoin/_delete_form.html.twig') }}
{% endblock %}
