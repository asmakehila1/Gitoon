{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_promo</th>
                <td>{{ promotion.idPromo }}</td>
            </tr>
            <tr>
                <th>Titre_promo</th>
                <td>{{ promotion.titrePromo }}</td>
            </tr>
            <tr>
                <th>Desc_promo</th>
                <td>{{ promotion.descPromo }}</td>
            </tr>
            <tr>
                <th>Image_promo</th>
                <td>{{ promotion.imagePromo }}</td>
            </tr>
            <tr>
                <th>Type_promo</th>
                <td>{{ promotion.typePromo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('promotion_index') }}">back to list</a>

    <a href="{{ path('promotion_edit', {'id_promo': promotion.id_promo}) }}">edit</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
