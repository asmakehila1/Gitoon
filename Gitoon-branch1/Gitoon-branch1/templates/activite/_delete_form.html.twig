<form method="post" action="{{ path('activite_delete', {'id_activite': activite.idActivite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite.idActivite) }}">
    <button class="btn">Supprimer</button>
</form>
